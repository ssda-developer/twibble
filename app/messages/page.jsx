import UnderConstruction from "@/components/UnderConstruction";

export default function MessagesPage() {
    return (
        <UnderConstruction
            title="Messages are under construction"
            description="Direct messages are not ready yet, but we're actively working on them."
        />
    );
}
